#include <iostream>
using namespace std; 

bool isPalindrome(const string& s, int p = 0);

void minMax(int* arr, int size, int& min, int& max, int pos);

string reverse(const string& s, int p = 0);

void printString(const string& s, int p = 0);